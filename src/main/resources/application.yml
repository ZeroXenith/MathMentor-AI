spring:
  application:
    name: mathmentor-ai

server:
  port: ${SERVER_PORT:18080}

ai:
  api-key: ${AI_API_KEY:${OPENAI_API_KEY:}}
  base-url: ${AI_BASE_URL:https://api.deepseek.com/chat/completions}
  model: ${AI_MODEL:deepseek-v4-flash}
