# Development profile — enable debug logging and H2 console
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    com.graduation.mathai: DEBUG

ai:
  mock-enabled: true
